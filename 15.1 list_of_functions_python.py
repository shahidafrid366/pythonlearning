"""
Functions are available Globally.
These are global functions available everywhere in Python without needing to call them
on an object.
They are not tied to any specific data type.

There are totally 69 built in functions Python

abs()          delattr()      hash()          memoryview()   set()
all()          dict()         help()          min()          setattr()
any()          dir()          hex()           next()         slice()
ascii()        divmod()       id()            object()       sorted()
bin()          enumerate()    input()         oct()          staticmethod()
bool()         eval()         int()           open()         str()
breakpoint()   exec()         isinstance()    ord()          sum()
bytearray()    filter()       issubclass()    pow()          super()
bytes()        float()        iter()          print()        tuple()
callable()     format()       len()           property()     type()
chr()          frozenset()    list()          range()        vars()
classmethod()  getattr()      locals()        repr()         zip()
compile()      globals()      map()           reversed()     __import__()
complex()      hasattr()      max()           round()

Categorized Built-in Functions
------------------------------
🔹 1. Input & Output
        print() → prints to console
        input() → takes user input


🔹 2. Type Conversion / Constructors
        int(), float(), complex()
        str(), bool()
        list(), tuple(), dict(), set(), frozenset()
        bytes(), bytearray(), memoryview()
        range() → sequence of numbers
        object() → base object type


🔹 3. Math & Numbers
        abs() → absolute value
        pow() → exponentiation (pow(x, y, mod))
        round() → round number
        min(), max() → smallest/largest
        sum() → sum of iterable
        divmod() → quotient + remainder


🔹 4. Sequences & Iterables
        len() → length
        enumerate() → index + value pairs
        zip() → combine iterables
        sorted() → sorted list
        reversed() → reverse iterator
        slice() → create a slice object
        map() → apply function to iterable
        filter() → filter iterable
        all() → True if all elements are True
        any() → True if any element is True


🔹 5. Object Introspection / Reflection
        type() → type of object
        id() → memory address
        isinstance() → check object type
        issubclass() → check subclass
        callable() → is object callable?
        dir() → attributes & methods of object
        vars() → __dict__ of object
        globals() → global namespace dict
        locals() → local namespace dict


🔹 6. Attribute Handling
        getattr() → get attribute
        setattr() → set attribute
        delattr() → delete attribute
        hasattr() → check if attribute exists
        property() → define property


🔹 7. Functional & Decorators
        staticmethod()
        classmethod()
        super() → access parent methods


🔹 8. Binary & Character Conversions
        bin() → int → binary string
        oct() → int → octal string
        hex() → int → hex string
        ord() → char → Unicode code
        chr() → Unicode code → char
        ascii() → printable ASCII representation


🔹 9. Code Execution & Compilation
        eval() → evaluate string as Python code
        exec() → execute Python code dynamically
        compile() → compile source to code object


🔹 10. Help & Debugging
        help() → interactive help
        breakpoint() → start debugger
        repr() → string representation


🔹 11. Imports
        __import__() → import module programmatically
"""
